import { Loader, LoaderWrapper } from "./style";

const PageLoader = () => {
  return (
    <LoaderWrapper>
      <Loader></Loader>
    </LoaderWrapper>
  );
};

export default PageLoader;
