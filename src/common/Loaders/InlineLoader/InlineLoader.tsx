import { Loader, LoaderWrapper } from "./style";

const InlineLoader = () => {
  return (
    <LoaderWrapper>
      <Loader></Loader>
    </LoaderWrapper>
  );
};

export default InlineLoader;
